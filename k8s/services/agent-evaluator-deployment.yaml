apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-evaluator
  namespace: linkedin-pipeline
  labels:
    app: agent-evaluator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-evaluator
  template:
    metadata:
      labels:
        app: agent-evaluator
    spec:
      containers:
        - name: agent-evaluator
          image: ghcr.io/maoroch/linkedin-ai-agent-evaluator:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4008
          envFrom:
            - configMapRef:
                name: pipeline-config
            - secretRef:
                name: pipeline-secrets
          env:
            - name: PORT
              value: "4008"
          resources:
            requests:
              cpu: 30m
              memory: 80Mi
            limits:
              cpu: 200m
              memory: 192Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 4008
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 4008
            initialDelaySeconds: 5
            periodSeconds: 10
